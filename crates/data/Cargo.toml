[package]
name = "galactic_explorer_data"
version = "0.7.0"
edition = "2021"
description = "Data persistence layer - save/load, serialization"

[dependencies]
galactic_explorer_core = { path = "../core" }
bevy = { version = "0.13", features = ["jpeg", "png"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
rand = "0.8"
thiserror = "1"
log = "0.4"