[package]
name = "galactic_explorer_core"
version = "0.7.0"
edition = "2021"
description = "Core ECS components, resources, and types"

[dependencies]
bevy = { version = "0.13", features = ["jpeg", "png", "hdr"] }
serde = { version = "1.0", features = ["derive"] }
rand = "0.8"
thiserror = "1"